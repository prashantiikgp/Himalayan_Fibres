template:
  slug: order_delivered_feedback
  name: "Order Delivered — Feedback Request"
  category: order_flow
  flow: order
  flow_step: 4
  subject_template: "How was your order, {{ first_name }}?"
  is_active: true
  required_variables:
    - first_name

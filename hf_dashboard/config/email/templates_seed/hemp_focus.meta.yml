template:
  slug: hemp_focus
  name: "Campaign — Hemp Yarn Focus"
  category: campaign_product
  subject_template: "Hemp yarn from the Himalayan hills, {{ first_name }}"
  is_active: true
  required_variables:
    - first_name
  optional_variables:
    - catalog_link
  variables:
    - name: catalog_link
      label: "Catalog URL"
      type: url
      placeholder: "https://www.himalayanfibres.com/catalog"
      example: ""
      required: false

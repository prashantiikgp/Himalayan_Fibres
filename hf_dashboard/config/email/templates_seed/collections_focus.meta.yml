template:
  slug: collections_focus
  name: "Campaign — Signature Collections Focus"
  category: campaign_product
  subject_template: "Our signature blend collections, {{ first_name }}"
  is_active: true
  required_variables:
    - first_name
  optional_variables:
    - catalog_link
  variables:
    - name: catalog_link
      label: "Catalog URL"
      type: url
      placeholder: "https://www.himalayanfibres.com/catalog"
      example: ""
      required: false

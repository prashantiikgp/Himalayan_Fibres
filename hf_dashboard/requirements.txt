# Upper bound on pandas to avoid HF-build hash-mismatch errors. Gradio
# floor bumped to 5.x (2026-05-05) — Gradio 4.44.x has multiple bugs that
# surface as 500 on / once the Pydantic schema graph grows: bool-schema
# crash in gradio_client.utils.get_type, and a starlette TemplateResponse
# signature mismatch that yields "unhashable type: 'dict'".
gradio>=5.0.0,<7.0.0
fastapi>=0.109.0
uvicorn>=0.27.0
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
pandas>=2.2.0,<3.0.0
openpyxl>=3.1.2
jinja2>=3.1.3
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
pyyaml>=6.0.1
Pillow>=10.2.0
email-validator>=2.1.0
python-dateutil>=2.8.2
python-dotenv>=1.0.1
requests>=2.31.0
# huggingface_hub 1.x dropped HfFolder/whoami imports that gradio 4.44.x
# still references. Until we remove the gradio import from api_v2's
# transitive chain (api_v2/routers/dashboard.py -> hf_dashboard/pages/home.py),
# stay on the 0.x line.
huggingface-hub>=0.24.0,<1.0.0
